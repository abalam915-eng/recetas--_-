@{
    ViewData["Title"] = "Mis Recetas";
    var borradores = ViewBag.Borradores as IEnumerable<RecetasApp.Models.Receta>;
    var publicadas = ViewBag.Publicadas as IEnumerable<RecetasApp.Models.Receta>;
}

<h1>Mis Recetas</h1>

<div class="mb-4">
    <a asp-action="Crear" class="btn btn-primary">Crear Nueva Receta</a>
</div>

<h3>En Borrador</h3>
<table class="table table-striped align-middle">
    <thead>
        <tr>
            <th>Título</th>
            <th>Autor Original</th>
            <th>Modificado por</th>
            <th>Fecha</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @if(borradores != null && borradores.Any())
        {
            foreach(var item in borradores)
            {
                <tr>
                    <td>@item.Titulo</td>
                    <td>@(item.AutorOriginal ?? "Yo")</td>
                    <td>Tú</td>
                    <td>@item.FechaCreacion.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>
                        <a asp-action="Editar" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Editar</a>
                        <form asp-action="Publicar" asp-route-id="@item.Id" method="post" style="display:inline;">
                            <button type="submit" class="btn btn-sm btn-success">Publicar Copia</button>
                        </form>
                        <form asp-action="Eliminar" asp-route-id="@item.Id" method="post" style="display:inline;" onsubmit="return confirm('¿Seguro que deseas borrar este borrador?');">
                            <button type="submit" class="btn btn-sm btn-danger ms-1">Borrar</button>
                        </form>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr><td colspan="5" class="text-center">No hay borradores.</td></tr>
        }
    </tbody>
</table>

<h3 class="mt-5">Publicadas</h3>
<table class="table table-striped align-middle">
    <thead>
        <tr>
            <th>Título</th>
            <th>Autor Original</th>
            <th>Modificado por</th>
            <th>Fecha Publicación</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @if(publicadas != null && publicadas.Any())
        {
            foreach(var item in publicadas)
            {
                <tr>
                    <td>@item.Titulo
                    @if(!string.IsNullOrEmpty(item.Imagen)){
                        <br><img src="@item.Imagen" class="img-thumbnail mt-1" style="max-height:60px;" />
                    }
                    </td>
                    <td>@(item.AutorOriginal ?? "Yo")</td>
                    <td>Tú</td>
                    <td>@item.FechaPublicacion?.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>
                        <form asp-action="Eliminar" asp-route-id="@item.Id" method="post" style="display:inline;" onsubmit="return confirm('¿Seguro que deseas borrar esta publicación?');">
                            <button type="submit" class="btn btn-sm btn-danger">Borrar</button>
                        </form>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr><td colspan="5" class="text-center">No has publicado recetas aún.</td></tr>
        }
    </tbody>
</table>
